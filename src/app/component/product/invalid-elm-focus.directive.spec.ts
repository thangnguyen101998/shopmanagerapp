import { InvalidElmFocusDirective } from './invalid-elm-focus.directive';

describe('InvalidElmFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidElmFocusDirective();
    expect(directive).toBeTruthy();
  });
});
